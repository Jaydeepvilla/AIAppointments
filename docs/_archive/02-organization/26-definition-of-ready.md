# Definition of Ready: Organization Management

Before development can begin:
- [x] Timezone selection component (e.g., `react-timezone-select`) is approved for use.
- [x] Database migrations for `organizations` and `businessSettings` tables are reviewed.
- [x] S3 bucket or Vercel Blob storage is provisioned for image uploads in staging.
- [x] UI mockups for the Business Hours "split shift" interface are finalized and approved by UX.
